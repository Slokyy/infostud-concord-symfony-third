{% set route = app.request.get('_route') %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link {{ 'drivers-all' in route ? 'active' }}" href="{{ path('drivers-all') }}">Drivers</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ 'vehicles-all' in route ? 'active' }}" href="{{ path('vehicles-all') }}">Vehicles</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ 'reservations-all' in route ? 'active' }}" href="{{ path('reservations-all') }}">Reservations</a>
        </li>
      </ul>
    </div>
  </div>

</nav>